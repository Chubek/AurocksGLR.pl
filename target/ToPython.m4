include(`support.m4')
define(<<<AUROCKS_C_SOURCE>>>, <<<# Generated by AurocksGLR (Python target).
# The grammar metadata is available to custom macro sets through the
# AUROCKS_* contract.  This conservative backend provides a documented API.
class Parser:
    def __init__(self, text):
        self.text = text
        self.pos = 0

    def parse(self):
        if self.text == "":
            return None
        raise SyntaxError("generated Python parser requires target-specific actions")

def parse(input):
    return Parser(input).parse()
>>>)
