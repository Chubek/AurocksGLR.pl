include(`support.m4')
define([[AUROCKS_C_SOURCE]], [[/* Generated by AurocksGLR (Go target).
The grammar's C prologue/actions are retained as documentation. */
package main

import "errors"

// Parse is a target-neutral entry point.  Implement semantic actions in Go
// or provide a custom target macro set for a production parser.
func Parse(input string) (any, error) {
	if input == "" { return nil, nil }
	return nil, errors.New("generated Go parser requires target-specific actions")
}
]])
